# Claude instructions

@AGENTS.md

`AGENTS.md` is the canonical project guide shared with Codex. Follow it first and update it when a durable repository rule changes. This file intentionally contains only Claude-specific direction so the two agents do not drift.

For a task that changes the site, run the relevant verification from `AGENTS.md` (normally `pnpm build`, plus `pnpm verify:media` for chapter-video work). Preserve unrelated uncommitted changes.
